# Median of two sorted arrays of different size
# Binary Search
# Median is a element such that half the elements are smaller than median.
# [1,2,3,4,5,6,7] => 4
# [1,2,3,4,5,6,7,8] => 4.5 => (4+5)/2
# Pending
def median(A,B):
    # Range of median is [mi,ma]
    mi = min(A[0],B[0])
    ma = max(A[-1],B[-1])
    while mi < ma:
        pass
        

